Your reminders:<br/>

<table class="table table-striped table-hover">
  <thead>
  <tr>
    <th>Date</th>
    <th>Reminder</th>
    <th>User</th>
    <th>Notified</th>
  </tr>
  </thead>
  <tbody>
  <% @reminders.each do |r| %>
    <tr>
      <td><%= r.date.to_s(:db) %></td>
      <td><%= r.text %></td>
      <td><%= r.user.username %></td>
      <td><%== '<span class="glyphicon glyphicon-ok"></span>' unless r.reminder_notifications.empty? %></td>
    </tr>
  <% end %>
  </tbody>
</table>

Your username is <%= @username %>